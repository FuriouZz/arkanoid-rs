{
  "verbose": false,
  "watch": [
    "src",
    "js",
    "examples"
  ],
  "events": {
    "start": "cargo build --target wasm32-unknown-unknown --example arkanoid",
    "restart": "cargo build --target wasm32-unknown-unknown --example arkanoid"
  },
  "ext": "html,js,rs"
}